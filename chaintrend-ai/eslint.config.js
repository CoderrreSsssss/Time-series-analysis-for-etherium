// Minimal flat ESLint config placeholder.
// This project uses `oxlint` (see package.json "lint" script) as its primary,
// zero-config linter for speed. This file is kept for structural compatibility
// with editors/CI that expect an eslint.config.js and can be expanded with
// `eslint-plugin-react` / `eslint-plugin-react-hooks` if you prefer ESLint.
export default [
  {
    ignores: ['dist/**', 'node_modules/**'],
  },
]
